import { Feedback } from "../../entities";
import { MissionsValues } from "../../widgets";

export const HomePage = () => {
    return (
        <div>
            <Feedback />
        </div>
    );
}

